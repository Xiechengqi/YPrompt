# 开发环境配置
# 本地开发时使用

# 后端API地址
VITE_API_BASE_URL=http://localhost:8888

# ==================== Debug 模式 ====================
# 启用 debug 模式（完全不依赖后端 API，使用 mock 数据）
# 设置为 'true' 启用，留空或 'false' 禁用
# 开发环境默认禁用，如需测试 UI 效果可启用
VITE_DEBUG_MODE=

# 登录用户配置（从环境变量读取，用于预填充登录表单）
# 注意：密码不会预填充，仅用于开发环境提示
VITE_LOGIN_USERNAME=admin
VITE_LOGIN_PASSWORD=admin123

# 注意：Linux.do OAuth 配置在后端，前端通过 /api/auth/config 获取

